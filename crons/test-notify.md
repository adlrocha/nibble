# test-notify

schedule = "*/2 * * * *"
enabled = true
skip_if_running = true
expires_at = "+5m"

## Prompt

This is a cron test. Reply with a single sentence: "Cron fired at <current time>." — no other output.
